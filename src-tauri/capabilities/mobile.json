{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only capabilities (biometric secure storage for the master key)",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": ["biometry:default"]
}
